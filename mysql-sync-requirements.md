# MySQL 数据同步工具 — 需求文档

> 本文档是该项目的唯一需求来源，面向零上下文的开发者/AI。所有决策已由需求方确认，标注"（默认，可改）"的条目除外。
> 建议开发流程：先输出完整设计方案（spec）经需求方确认，再编写实施计划，最后 TDD 实施。

## 1. 背景与目标

需求方有**开发库、测试库、生产库**三套 MySQL，连接各不相同。数据需要同步时目前靠人工复制，费时易错。

目标：做一个独立的 Python 项目，实现 MySQL 表数据同步。**一套同步引擎，两副面孔**：

1. **Web GUI**：浏览器访问的本地页面，需求方手动可视化操作同步；
2. **MCP stdio server**：暴露给另一个已存在的 agent 项目（MySmallAgent）调用，最终效果是需求方在 QQ 里对 agent 说一句"把 A 连接 X 库 Y 表的数据增量同步到 B 连接 M 库 N 表"，agent 通过 MCP 调用本工具完成同步。

两副面孔共用同一套引擎代码，只是入口不同（类比一个后端同时有 Web 层和 RPC 层）。

## 2. 与 MySmallAgent 的接入契约（重要，不可偏离）

MySmallAgent 侧的 MCP client 已经完成并验证，本项目**无需也无法改动 agent 侧**。契约如下：

- **传输方式：stdio**。agent 每次调用工具时会把本项目的 MCP server 当**子进程拉起**（连接→调用→断开，不常驻）。因此 MCP server 入口必须是"启动快、无状态、每次进程内完成一次请求即可退出"的设计。
- agent 侧通过它自己项目里的 `mcp.json` 接入，格式（示例，写文档用，不放本项目）：
  ```json
  {
    "mcpServers": {
      "mysql-sync": {
        "command": "uv",
        "args": ["--directory", "<本项目绝对路径>", "run", "python", "mcp_server.py"]
      }
    }
  }
  ```
- agent 会把工具注册为 `mcp_mysql-sync_{工具名}`，并将**所有 MCP 工具视为危险工具**；但在 QQ 场景下 agent **自动批准**所有调用——**因此一切安全护栏必须在本项目（server 侧）实现，不能指望 agent 拦截**。
- 工具的 `description` 和参数 JSON Schema 会原样展示给 LLM，务必写清晰（中文），LLM 靠它决定怎么调用。
- 推荐用官方 `mcp` SDK 的 `FastMCP` 写 server，最小模板（已在 agent 侧验证可连通）：
  ```python
  from mcp.server.fastmcp import FastMCP

  mcp = FastMCP("mysql-sync")

  @mcp.tool()
  def some_tool(arg: str) -> str:
      """工具描述（给 LLM 看）。"""
      ...

  if __name__ == "__main__":
      mcp.run()  # 默认 stdio 传输
  ```
- 形态参考项目：https://github.com/osmanahmadxai/SYNCLE

## 3. 功能需求（已确认的决策）

| # | 需求 | 已确认决策 |
|---|------|-----------|
| 1 | 同步语义 | 源连接/库/表 → 目标连接/库/表，单表为单位 |
| 2 | 增量判定 | **主键逐行比对**：按主键比对源和目标，找出目标缺失的行（新增）与内容不同的行（变更）。引擎须分批处理（batch），兼顾较大的表 |
| 3 | 冲突策略 | **upsert 覆盖**：目标已有同主键行时用源数据覆盖（`INSERT ... ON DUPLICATE KEY UPDATE`） |
| 4 | 连接管理 | **连接在 Web GUI 里添加/编辑/删除**，持久化在本项目本地（形式由设计定，如加密 JSON/SQLite）；**配置文件不预置任何默认连接**；**MCP 面孔只能按别名引用已存在的连接**，不能新建连接、不接受任意连接串 |
| 5 | GUI 形态 | **Web 页面**（本地起服务、浏览器访问；技术选型由设计阶段定，如 FastAPI+简单前端 / Streamlit） |
| 6 | 删除行为 | 源已删除而目标仍存在的行：**不删除、不处理**（本工具只做新增+覆盖，不做删除同步） |
| 7 | DDL | **不同步表结构**。目标表不存在或主键结构不一致时**报错并给出清晰提示**，不自动建表/改表（默认，可改：若需求方在设计阶段要求"目标表不存在时自动建表"，按其要求调整） |

## 4. MCP 工具集（建议清单，设计阶段可细化）

| 工具 | 作用 |
|------|------|
| `list_connections` | 列出已配置的连接别名（**不返回凭据**） |
| `list_tables` | 列出某连接某库下的表 |
| `preview_sync`（dry-run） | 按参数（源/目标连接别名、库、表、可选 WHERE 条件）做比对，**只返回**将新增/覆盖的行数、主键范围示例，**不写任何数据** |
| `execute_sync` | 真正执行同步；写入受保护连接时必须显式携带确认参数（见 §5） |

工具参数一律使用**连接别名**，凭据永不出现在参数或返回值中。

## 5. 安全护栏（必须实现，理由见 §2：agent 侧自动批准，拦不住）

1. **dry-run 优先**：`preview_sync` 与 `execute_sync` 分离；工具描述中明确引导 LLM"先 preview 汇报给用户，再 execute"。
2. **受保护连接**：GUI 添加连接时可勾选"受保护"（如生产库）。向受保护连接**写入**时：
   - MCP：`execute_sync` 必须显式携带 `confirm=true` 之类的确认参数，否则拒绝并返回提示；
   - GUI：弹出二次确认。
3. **凭据安全**：凭据只存本项目本地（不进 git，`.gitignore` 覆盖）；任何工具返回值、日志、错误信息中不得包含密码。
4. **只读连接不受限**：从任何连接**读**（比对、预览）不需要确认。

## 6. 非功能需求

- Python 3.11+；包管理建议 `uv`（与 agent 项目一致）。
- 提供 **Dockerfile**（为后续全容器化部署做准备，见 §8）。
- 单元测试覆盖同步引擎核心逻辑（比对、分批、upsert 语句生成、护栏判定）；数据库交互测试可用 mock 或本地 MySQL。
- 代码注释/文档用中文。

## 7. 本迭代明确不做

- 定时/自动触发同步（全部由人或 agent 主动发起）；
- 异构数据库（仅 MySQL → MySQL）；
- 删除同步（见 §3-6）；
- 表结构（DDL）同步（见 §3-7）；
- MCP 的 HTTP/SSE 传输（下个迭代，见 §8）。

## 8. 演进路径（写给设计者，避免架构堵死后路）

- **本迭代**：同步引擎 + Web GUI + stdio MCP，本地跑通"QQ → agent → MCP → 同步完成"全链路。
- **下个迭代**：全部 docker 化。届时本项目将以**常驻容器 + HTTP 传输**方式提供 MCP（agent 侧再加 HTTP 支持）。设计时请保证：同步引擎与传输层解耦，未来加 HTTP 入口不需要改引擎。

## 9. 验收标准

1. Web GUI：能添加连接（含受保护标记）→ 选择源/目标库表 → 预览差异 → 执行同步 → 看到结果统计；
2. MCP：agent 项目的 `mcp.json` 加一条配置后，`/tools` 里出现本项目的工具；
3. 端到端：在 QQ 里对 agent 说"把 dev 连接 a 库 t 表增量同步到 test 连接 b 库 t 表"，agent 先调 preview 汇报、再调 execute 完成，数据正确落库；
4. 护栏：目标为受保护连接且未带确认参数时，execute 被拒绝并返回可读提示；
5. 引擎核心逻辑测试全绿。
